todo:
- ajouter commentaire au code !!
- separate HttpRequest into multiple files HttpRequestLine/HttpRequestHeaders/HttpRequestBody
- finir le parsing incomplet dans HttpRequest
- check end of header (\r\n\r\n) and content-length for requests that don't end explicitely
- parse la config (port, host, root)
- integration test
- cgi basique
- DELETE, POST for files
- consider checking event flag for master socket
- add logger with levels

- make EventPool less generic. Instead of observe, there should be addMaster, addClient, addScript.
- add a muteClient to EventPool, that disables the reading flags
- ajouter un flag pour le PORT (sinon les tests run en parallele sur le même port...)